// eslint-disable-next-line @typescript-eslint/no-var-requires
const subscriber = require("../subscriber.secrets.json")
// eslint-disable-next-line @typescript-eslint/no-var-requires
const publisher = require("../publish.secrets.json")
// eslint-disable-next-line @typescript-eslint/no-var-requires
const secrets = require("../secrets.json")

export {
  subscriber,
  publisher,
  secrets
}
